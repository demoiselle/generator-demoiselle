/*
describe('angular-4App E2E test', () => {
  beforeEach(() => {
    browser.get('/');
  });

  it('should have a title', () => {
    expect(browser.getTitle()).toEqual('<%= project.capital %> App');
  });

  it('should have <tov-nav>', () => {
    expect(element(by.css('<%= project.kebab %>-app top-nav')).isPresent()).toBe(true);
  });

  it('should have <sidebar-menu>', () => {
    expect(element(by.css('<%= project.kebab %>-app sidebar-menu')).isPresent()).toBe(true);
  });

  it('should have a main title', () => {
    expect(element(by.css('#main h1')).getText()).toEqual('<%= project.capital %> App');
  });

  it('should have a main div', () => {
    expect(element(by.css('<%= project.kebab %>-app div#main')).isPresent()).toBe(true);
  });
});
*/