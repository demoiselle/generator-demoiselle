{
  "name": "<%= project.lower %>-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.0",
    "axios": "^1.7.0",
    "@vueuse/core": "^10.7.0"<% if (typeof npmDeps !== 'undefined') { var npmKeys = Object.keys(npmDeps); for (var i = 0; i < npmKeys.length; i++) { %>,
    "<%= npmKeys[i] %>": "<%= npmDeps[npmKeys[i]] %>"<% } } %>
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
